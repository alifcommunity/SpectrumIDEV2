#ألف

دالة نص إدخال(نص أ)
    نص م
	اطبع(أ)

	--لحقن لغة ++C في لغة ألف نقوم بتضمين شفرة سي كالتالي "_س_ (لغة سي) _س_"
    _س_
    std::getline (cin, @م@);
    _س_

    إرجاع م
نهاية دالة	

دالة رئيسية
	نص ب = إدخال("ما هو اسمك؟ ")
	اطبع("اهلاً بك " + ب + سطر)
نهاية دالة